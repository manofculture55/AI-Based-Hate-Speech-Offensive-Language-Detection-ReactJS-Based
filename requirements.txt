# =============================================================
# Hate Speech Detection
# Direct dependencies, pinned to the versions this project is
# known to run on (Python 3.10, Windows/CPU).
#
#   pip install -r requirements.txt
#
# These are the packages the code imports directly. For a fully
# reproducible environment including every transitive dependency,
# install from the lock file instead:
#
#   pip install -r requirements.lock.txt
#
# Pins are exact (==) on purpose. The previous >= ranges meant two
# installs a month apart could resolve to different majors -- and
# numpy 2.x in particular breaks the pinned TensorFlow build.
# =============================================================

# -------------------------------------------------------------
# Core ML & Data
# -------------------------------------------------------------
numpy==1.26.4            # <2.0: TensorFlow 2.21 is built against the 1.x ABI
pandas==2.3.3
scikit-learn==1.7.2
tensorflow-cpu==2.21.0
keras==3.12.4
h5py==3.14.0             # required to load the .h5 model files
joblib==1.5.3
openpyxl==3.1.5          # reads the .xlsx dataset in normalize.py

# -------------------------------------------------------------
# NLP & Text Processing
# -------------------------------------------------------------
nltk==3.10.3
regex==2026.7.19
transformers==5.15.0
tf-keras==2.21.0         # Keras 2 shim that transformers still expects

# -------------------------------------------------------------
# Backend (REST API)
# -------------------------------------------------------------
Flask==3.1.3
flask-cors==6.0.5
Werkzeug==3.1.8          # pinned explicitly: Flask's HTTP layer, used directly
                         # by the error handlers and file uploads
python-dotenv==1.2.3     # loads .env (API keys, CORS origins, ports)
marshmallow==4.3.1       # request body validation
waitress==3.0.2          # production WSGI server (works on Windows)

# -------------------------------------------------------------
# Reporting & Utilities
# -------------------------------------------------------------
matplotlib==3.10.9       # confusion matrix plots written during training
seaborn==0.13.2
tqdm==4.70.0
